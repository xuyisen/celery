name: Linter

on: push

jobs:
  linter:
    runs-on: ubuntu-22.04
    steps:

    - name: Checkout branch
      uses: actions/checkout@v5

    - name: Run pre-commit
      uses: pre-commit/action@v3.0.1
